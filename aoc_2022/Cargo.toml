[package]
name = "aoc2022"
version = "1.0.0"
edition = "2021"

[dependencies]
ahash = "0.8"
anyhow = "1"
clap = { version = "4", features = ["color", "derive"]}
framework = { path = "../framework" }
itertools = "0.10"
nom = "7"

[dev-dependencies]
criterion = "0.4"
criterion-macro = "0.4"

[features]
criterion = []
